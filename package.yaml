name:                information-gtk
version:             0.1.0.0
github:              "githubuser/information-gtk"
license:             GPL-3
author:              ""
maintainer:          ""
copyright:           ""

# extra-source-files:
# - README.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/githubuser/byebye#readme>

dependencies:
- base >= 4.7 && < 5
- directory
- gi-gtk == 3.0.*
- gi-gdk
- gi-gtk-hs
- haskell-gi-base
- process
- unix
- text
- aeson
- aeson-casing
- servant
- servant-server
- wai
- warp
- lens-aeson
- req

library:
  source-dirs: src

executables:
  information-gtk:
    main:                Main.hs
    source-dirs:         src
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - information-gtk

tests:
  information-gtk-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - information-gtk
